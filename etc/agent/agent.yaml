server:
  http_listen_address: 0.0.0.0
  http_listen_port: 3100
  grpc_listen_address: 0.0.0.0
  grpc_listen_port: 9095
  log_level: info

metrics:
  wal_directory: ${AGENT_WAL_DIR}

logs:
  positions_directory: ${SNAP_COMMON}/positions
